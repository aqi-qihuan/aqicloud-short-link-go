# 2026-05-03

## 短链重定向压测完成

### 环境
- MySQL: midware-mysql (password: aqi1015!)
- 服务: Gateway(:8888) + Link(:8003) 本地编译运行
- k6 v1.7.1 via brew install

### 测试结果
| 指标 | 数值 |
|------|------|
| 总请求 | 780,324 |
| QPS | 5,201 |
| P50 | 18ms (含429) / 49ms (302) |
| P95 | 80ms (含429) / 346ms (302) |
| P99 | 141ms |
| 限流触发率 | 98% |

### 发现的关键问题
1. Gateway 限流 (100r/s/IP) 98% 请求被 429 — 限流器正常工作
2. 成功 302 响应 P50=49ms — MySQL 直查无缓存，高并发下延迟上升
3. 限流器 sync.Map 无 TTL 清理
4. seed_data.py code 字段 bug 已修复（原来去掉了 db_prefix/table_suffix）

### 修复
- seed_data.py: code 字段从 `code_inner` 改为 `code_full`（完整码）

### 数据库密码
- midware-mysql: root / aqi1015!

## 性能优化实施完成

### 实施的 3 项优化
1. **热路径 Redis 缓存** — link_api.go Cache-Aside (HGETALL/HSET, 10min TTL, 空结果 1min TTL 防穿透)
2. **Gateway Proxy 复用** — url.Parse + NewSingleHostReverseProxy 移到闭包外
3. **GORM 连接池** — MaxOpenConns=100, MaxIdleConns=20, ConnMaxLifetime=5min

### 修改的文件
- `cmd/gateway/main.go` — reverseProxy/stripPrefixProxy 复用 proxy 实例
- `cmd/link/main.go` — GORM 连接池配置 + 注入 rdb 到 LinkApiController
- `internal/link/controller/link_api.go` — 完整重写，新增 Redis Cache-Aside
- `internal/link/service/short_link_service.go` — 删除/更新后失效 Redis 缓存
- `internal/common/constant/redis_key.go` — 新增 SHORT_LINK_CACHE_KEY 常量

### 压测对比结果 (800 VU, 150s)
| 指标 | 优化前 | 优化后 | 改善 |
|------|--------|--------|------|
| 302 P50 | 49.63ms | 32.17ms | -35% |
| 302 P95 | 346.06ms | 153.65ms | -56% |
| 302 最大 | 1,957ms | 526ms | -73% |
| QPS | 5,201 | 5,605 | +7.8% |
| 单请求缓存命中 | - | ~5ms | vs DB 20ms (4x) |

### 环境信息
- MySQL: midware-mysql (root / aqi1015!)
- Redis: redis container (password: aqi1015)
- Gateway: :8888, Link: :8003
- Kafka: 未运行 (Kafka 日志报 connection refused 但不影响核心功能)

## 第二轮优化 (4项)

### 实施内容
1. **限流阈值**: 100r/s → 1000r/s, burst 2000
2. **Redis 缓存预热**: 启动时批量加载 1200 条热点短链到 Redis (Pipeline)
3. **gin.New()**: Gateway 去掉 Logger 中间件
4. **Kafka BatchTimeout**: 10 (10ns!) → 100ms + BatchSize=100

### 三轮压测结论
- **Round 1 → Round 2**: 302 P50 49ms→32ms (-35%), P95 346ms→154ms (-56%)
- **Round 2 → Round 3**: 302 成功数 15K→120K (8x)，但 1200VU 导致后端过载
- **新瓶颈暴露**: Link 单实例处理能力不足 (1200 VU 下 P95=2s)
- **缓存命中**: 稳定 2-3ms，预热有效
- **下一步**: 水平扩展 Link 多实例 + 负载均衡

### 修改文件汇总 (共 6 个)
- cmd/gateway/main.go — Proxy复用 + gin.New() + 限流1000
- cmd/link/main.go — 连接池 + rdb注入 + 缓存预热
- internal/link/controller/link_api.go — Redis Cache-Aside
- internal/link/service/short_link_service.go — 缓存失效
- internal/common/constant/redis_key.go — 缓存key常量
- internal/common/mq/kafka.go — BatchTimeout修复
